Flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
pyttsx3==2.90
gtts==2.5.3
pydub==0.25.1

# pyttsx3 backends per OS:
#   Windows -> pywin32 (SAPI5)
#   macOS   -> pyobjc (NSSpeechSynthesizer)
#   Linux   -> requires espeak or espeak-ng system package
#
# pydub also requires ffmpeg binary installed and on PATH
# If ffmpeg is missing, backend will fall back to WAV output.
